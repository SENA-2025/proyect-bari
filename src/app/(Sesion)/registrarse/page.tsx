import type { Metadata } from "next";

// Metadata
export const metadata: Metadata = {
	title: "Registrarse",
};

// Componentes

export default function Register_Page() {
	return (
		<div>
			{/* Formulario */}
			<div></div>
		</div>
	);
}
